<p style="color: green"><%= notice %></p>

<% content_for :title, "Flights" %>

<h1>Flights</h1>
<%= render 'form' %>
<div id="flights">
    <%= simple_form_for :flight, url: new_booking_path, method: :get do |f|%>
      <%= f.collection_radio_buttons :id, @flights, :id, :flight_name%>
      <%= f.submit%>
    <%end%>
</div>

<%= link_to "New flight", new_flight_path %>
